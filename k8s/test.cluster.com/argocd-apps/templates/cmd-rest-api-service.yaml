apiVersion: v1
kind: Service
metadata:
  name: {{ template "cmd-rest-api.fullname" . }}
  labels:
    app: {{ template "cmd-rest-api.name" . }}
    chart: {{ template "cmd-rest-api.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "cmd-rest-api.name" . }}
    release: {{ .Release.Name }}